# Copy to .env and set real values
# Example Postgres DATABASE_URL (change user/password/host/port/dbname as needed)
DATABASE_URL=postgres://user:password@localhost:5432/pocketledger
JWT_SECRET=your_jwt_secret_here
PORT=8080
